# AICOS admin (apps/admin) — local env template. Copy to .env.local.
# Ports are locked platform-wide; see the repo root .env.example.

# Base URL of the AICOS API (locked to port 4000). The login stub posts to
# `${NEXT_PUBLIC_API_URL}/api/v1/auth/login`.
NEXT_PUBLIC_API_URL=http://localhost:4000
